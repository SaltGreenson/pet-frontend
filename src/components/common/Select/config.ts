type SizeType = {
  maxHeight: string;
  width: string;
};
export const dimensionConfig = {
  little: {
    maxHeight: "36px",
    width: "5rem",
  } as SizeType,
  large: {
    maxHeight: "36px",
    width: "100%",
  } as SizeType,
};
